import { ReaderTaskEither } from '../ReaderTaskEither';
import { UserQuest } from './type';
import { Config } from '../Config/type';
declare type Env = {
    cap: unknown;
    config: Config;
};
declare type Error = string;
declare type Effect<A> = ReaderTaskEither<Env, Error, A>;
declare const main: Effect<UserQuest>;
export default main;
