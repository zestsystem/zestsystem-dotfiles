import { ReaderTaskEither } from 'fp-ts/lib/ReaderTaskEither';
import { SimpleSpawnResult } from './simple-spawn';
import * as SIS from './simple-spawn';
declare type Error = string;
declare type Effect<A> = ReaderTaskEither<Record<string, unknown>, Error, A>;
declare type Capabilities = {
    mkDir: (path: string, opts: {
        recursive: boolean;
    }) => Effect<void>;
    writeFile: (path: string, content: string) => Effect<void>;
    readFile: (path: string) => Effect<string>;
    spawn: (command: 'yarn' | 'npm' | 'git', args: string[], opts: SIS.Options) => Effect<SimpleSpawnResult>;
};
declare const capabilities: Capabilities;
export { capabilities, Capabilities };
