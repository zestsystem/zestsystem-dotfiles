import { ReaderTaskEither } from 'fp-ts/lib/ReaderTaskEither';
import { SimpleSpawnResult } from './simple-spawn';
import * as SIS from './simple-spawn';
declare type Error = string;
declare type Effect<A> = ReaderTaskEither<Record<string, unknown>, Error, A>;
declare const mkDir: (path: string, opts: {
    recursive: boolean;
}) => Effect<void>;
declare const writeFile: (path: string, content: string) => Effect<void>;
declare const readFile: (path: string) => Effect<string>;
declare const spawn: (command: string, args: string[], opts: SIS.Options) => Effect<SimpleSpawnResult>;
export { mkDir, writeFile, readFile, spawn };
