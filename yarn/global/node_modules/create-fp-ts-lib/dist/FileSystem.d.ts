import * as TE from 'fp-ts/lib/TaskEither';
import { FileObj } from './FileObj';
import { Config } from './Config/type';
import { Capabilities } from './Capabilities';
declare type FileSystem = Record<string, FileObj>;
declare type Env = {
    cap: Capabilities;
    config: Config;
    files: FileSystem;
};
declare type Error = string;
declare type Effect<A> = (env: Env) => TE.TaskEither<Error, A>;
declare const writeOut: Effect<void>;
export { FileSystem, writeOut };
