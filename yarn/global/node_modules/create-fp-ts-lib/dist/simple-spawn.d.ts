/// <reference types="node" />
import * as cp from 'child_process';
export declare type SimpleSpawnResult = {
    stdout: string;
    stderr: string;
    exitCode: number | null;
};
export declare type Options = Omit<cp.SpawnOptions, 'stdio'>;
export declare const simpleSpawn: (command: string, args: string[], options: Options, cb: (err: null | NodeJS.ErrnoException, r?: SimpleSpawnResult) => void) => void;
